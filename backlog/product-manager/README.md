# Product Manager Workspace

This folder stores Product Manager outputs for `splendor_dual_pretzel`.

- `latest.md` is the current summary for the human user.
- `screenshots/` stores the latest screenshot set and supporting notes.
